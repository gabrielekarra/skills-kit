{
  "name": "@skills-kit/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "skills-kit": "dist/index.js"
  },
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint src test --max-warnings=0",
    "pretest": "pnpm -C ../core build",
    "test": "vitest run"
  },
  "dependencies": {
    "@skills-kit/agent": "workspace:*",
    "@skills-kit/core": "workspace:*",
    "chalk": "^5.3.0",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.30"
  }
}
